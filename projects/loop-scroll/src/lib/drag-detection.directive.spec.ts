import { DragDetectionDirective } from './drag-detection.directive';

describe('DragDetectionDirective', () => {
  it('should create an instance', () => {
    const directive = new DragDetectionDirective();
    expect(directive).toBeTruthy();
  });
});
